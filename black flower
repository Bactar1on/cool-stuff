<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            background-color: white;
            overflow: hidden;
            margin: 0;
        }

        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .flower {
            width: 100px;
            height: 100px;
            background-color: black;
            border-radius: 50%;
            position: relative;
            animation: sway 2s infinite alternate, grow 5s forwards;
        }

        .petals {
            width: 100%;
            height: 100%;
            background-color: red;
            border-radius: 50%;
            position: absolute;
        }

        @keyframes sway {
            0% {
                transform: rotate(0deg);
            }
            100% {
                transform: rotate(10deg);
            }
        }

        @keyframes grow {
            0% {
                transform: scale(0);
            }
            100% {
                transform: scale(1);
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="flower">
            <div class="petals"></div>
        </div>
    </div>
</body>
</html>
